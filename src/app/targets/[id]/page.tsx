import Container from "@/components/container";
import TargetTransaction from "@/components/target/target-transaction";

const Page = () => {
  return (
    <Container>
      <TargetTransaction />
    </Container>
  );
};

export default Page;
